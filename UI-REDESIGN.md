# 聊天档案工作台 · UI 重构

## 设计方向

纸白底、松绿色强调、细描边、安静的导航与清楚的阅读层级。品牌插画集中在总览，其他页面让出空间给真实内容。七个页面共用标题、按钮、对话框、头像、分页、状态面板及 CSS token。

主要色：背景 #f7f8f5，面板 #ffffff，正文 #26362e，强调 #386b56。深色主题由同名 token 覆盖。字体使用系统字体；不提交字体文件。图标是 `src/lib/icons.js` 内 39 枚原创 SVG 路径，24×24 网格、1.7px 线宽；并非 image 2.5 生成素材。

## 布局与组件

- 总览：基础摘要、趋势、类型、最近会话和快捷入口。
- 聊天：桌面双栏；手机上分开切换会话列表与消息；主搜索常驻，低频条件放到面板。
- 联系人与会话：紧凑列表；群聊：空间卡片；媒体：画廊，按内容类型区别组织。
- 数据分析：所有指标使用同一时间范围，明确样本来源，不混入虚构回复率。

样式入口 `src/styles/main.css` 按顺序引入四个样式模块。01 为 token、基础组件和外壳；02 为内容布局；03 为媒体、弹窗与桌面断点；04 为手机断点与细节覆盖。修改时注意后续覆盖顺序。

## 功能与数据边界

API 适配同时兼容已有 JSON、CSV 和旧文本格式，优先请求 JSON。会话与联系人跳转保留稳定 ID。异步请求使用序号保护，连接变更会清空页面内缓存。加载失败不会偷偷展示演示数据。

图表和媒体统计最多扫描最近 10 个会话、每个 1,000 条；UI 标明覆盖范围、失败和截断。追加多关键词只筛选当前页。导出区分当前页与最多 5,000 条查询结果，CSV 转义引号、换行和公式前缀。

`?demo=1` 是显式演示入口，人物、内容与素材均为虚构。页面仍会加载当前站点静态资源，但不会调用 chatlog 后端。真实模式没有经过私人服务联调，后端字段适配需按实际版本验收。

## 工程接入

七页路由不变，仍使用 Vue CLI；不自动部署、不改变后端。生产 publicPath 改为绝对 `/`，避免 history 路由刷新时相对资源路径错误；子目录通过 VUE_APP_PUBLIC_PATH 配置。移除旧配置中不存在的 SCSS 全局文件注入，新 UI 直接使用 CSS。

入口不再全局加载 Element Plus、图标集合、动画库和 Vuex，但暂时保留原依赖清单与旧模块，避免扩大迁移范围。README 和英文 README 使用 `images/ui/` 中的运行截图；原说明存入 `docs/legacy/`。

测试流程与截图来源见 [TEST-REPORT.md](TEST-REPORT.md)。
