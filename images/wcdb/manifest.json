{
  "fictional": true,
  "renderer": "production dist + actual SQLite WASM + OPFS",
  "sourceCommit": "509efbfc1b4bb589417f1612a16b3659a0478ea5",
  "checks": [
    "real SQLite import: two message shards, contacts, sessions, compression report",
    "historical archive extent is recorded",
    "composite IDs prevent shard collisions and preserve 64-bit server IDs",
    "Zstd content decoded and sender identities mapped per shard",
    "two-character Chinese search reaches both shards beyond first page",
    "literal wildcard characters and full-index AND/OR filters",
    "stable pagination across equal timestamps",
    "statistics use the complete imported index rather than ten-session sampling",
    "local source desktop route: dashboard",
    "local source desktop route: chatlog",
    "local source desktop route: analytics",
    "local source desktop route: contacts",
    "local source desktop route: chatrooms",
    "local source desktop route: sessions",
    "local source desktop route: media",
    "HTML-like SQLite message content remains inert visible text",
    "chat UI passes reactive query arrays safely to the Worker",
    "local query exports a real CSV file",
    "explicit local attachment association and safe preview",
    "archive and attachment persistence after reload without backend requests or uploads",
    "invalid replacement leaves the previous archive intact",
    "valid SQLite with an unsupported schema is not treated as empty history",
    "cancellation after import progress preserves the published archive",
    "nonempty WAL blocks import without replacing existing data",
    "HTTP/local switching clears view caches without deleting the local archive",
    "390px mobile source manager has no horizontal overflow",
    "explicit clear removes the local index and associated attachments",
    "no uncaught browser exceptions"
  ],
  "browserErrors": []
}